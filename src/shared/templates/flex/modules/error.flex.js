const atoms = require("../../flex/components/base-ui");
const { buildBubble } = require("../../flex/layouts/base-layout");

function beaconNotFoundFlex() {
  return atoms.makeFlex("ไม่พบสัญญาณ Beacon", {
    ...buildBubble({
      title: { text: "⚠️ ไม่สามารถลงเวลาได้", color: "#FF3333" },
      subTitle: { text: "ท่านไม่อยู่ในบริเวณจุดลงเวลา", color: "#FF3333" },
      contents: [
        atoms.baseText({
          text: "เนื่องจากท่านไม่ได้อยู่ในพื้นที่ โปรดตรวจสอบว่าท่านอยู่ในบริเวณจุดลงเวลาแล้ว หากท่านอยู่ในพื้นที่แล้วแต่ยังพบข้อผิดพลาดนี้",
        }),

        atoms.boxColumns({
          contents: [
            atoms.baseText({
              text: "กรุณาลองทำตามคำแนะนำดังต่อไปนี้:",
              weight: "bold",
              size: "sm",
              color: "#222222",
              margin: "sm",
            }),

            atoms.buildBullet("📱 1. เปิด Bluetooth ในโทรศัพท์ของท่าน"),
            atoms.buildBullet("🔄 2. ปิด App LINE แล้วเปิดใหม่อีกครั้ง"),
            atoms.buildBullet("📞 3. หากยังพบปัญหา กรุณาติดต่อฝ่ายบุคคล"),
          ],
        }),
      ],
    }),
  });
}

const noShiftFlex = () =>
  atoms.makeFlex("ไม่พบกะการทำงาน", {
    type: "bubble",
    body: {
      type: "box",
      layout: "vertical",
      contents: [
        atoms.baseText({
          text: "📅 ไม่พบตารางงาน",
          weight: "bold",
          color: "#FF9900",
          size: "xl",
        }),
        atoms.baseText({
          text: "ระบบไม่พบข้อมูลกะการทำงานของคุณในวันนี้ กรุณาติดต่อ HR หรือหัวหน้างาน",
          wrap: true,
          margin: "md",
        }),
      ],
    },
  });

module.exports = { beaconNotFoundFlex, noShiftFlex };
